# AWS Credentials Integration

This example demonstrates how to use AWS credentials stored in Airflow connections with SkyPilot tasks.

## Prerequisites:
- Create an AWS connection named `skypilot_aws_task` in Airflow. Refer to [Accessing private resources from remote clusters](../../README.md#optional-accessing-private-resources-from-remote-clusters) for more details
