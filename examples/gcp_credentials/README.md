# GCP Credentials Integration

This example demonstrates how to use GCP credentials stored in Airflow connections with SkyPilot tasks.

## Prerequisites:
- Create a GCP connection named `skypilot_gcp_task` in Airflow. Refer to [Accessing private resources from remote clusters](../../README.md#optional-accessing-private-resources-from-remote-clusters) for more details
